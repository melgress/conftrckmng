package main;
import java.io.*;
import java.util.Scanner;

public class Ctm_main {

	public static void main(String[] args) {
		
	
		
	
		try {

		
			Reader reader =
				    new BufferedReader(
				        new FileReader("test3"));
			

				int theCharNum = reader.read();
				Boolean isNumber = Character.isDigit(str.charAt(theCharNum));
				while(theCharNum != -1) {
					char theChar = (char) theCharNum;
					if (isNumber) {
				    

				    System.out.print(theChar);
					
					
				    theCharNum = reader.read();
				    
					}
				    
					
					else {
						System.out.println("Zeit  \n " + theChar);
					}
				}
		    
		        
		} catch (IOException ex) {
		    ex.printStackTrace();
		}

	

}
}

/*
 * // create a reader instance
		    BufferedReader br = new BufferedReader(new FileReader("test3"));

		    // read until end of file
		    String letter;
		    while ((letter = br.readLine()) != null) {
		        System.out.println(letter);
		    }

		    // close the reader
		    br.close();
		    */
